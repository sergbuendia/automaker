# Тестовый скрипт для проверки аутентификации Claude CLI

Этот скрипт позволяет независимо от основного проекта проверить, как работает аутентификация через Claude CLI и сделать тестовый вызов SDK.

## Установка

### Вариант 1: Установка зависимостей вручную

```bash
npm install @anthropic-ai/claude-agent-sdk
```

### Вариант 2: Использование package.json

```bash
# Скопируйте test-claude-auth-package.json в package.json
cp test-claude-auth-package.json package.json
npm install
```

## Запуск

```bash
node test-claude-auth.js
```

## Что проверяет скрипт

1. **Установка Claude CLI** - проверяет, установлен ли CLI и его версию
2. **Аутентификация CLI** - проверяет, авторизован ли CLI через команду `claude whoami`
3. **Файлы конфигурации** - ищет файлы с учетными данными CLI (OAuth токены, API ключи)
4. **Переменные окружения** - проверяет наличие `ANTHROPIC_API_KEY`
5. **Тестовый вызов SDK** - делает реальный запрос к Claude API через SDK

## Результаты

Скрипт покажет:

- ✓ Установлен ли Claude CLI
- ✓ Аутентифицирован ли CLI
- ✓ Найдены ли файлы конфигурации
- ✓ Какой метод аутентификации будет использован (CLI или API ключ)
- ✓ Результат тестового вызова SDK

## Пример вывода

```
🔍 Тестирование аутентификации Claude CLI и SDK

============================================================
1. Проверка установки Claude CLI
============================================================
✓ Claude CLI найден: /usr/local/bin/claude
✓ Версия: claude 1.2.3

============================================================
2. Проверка аутентификации CLI
============================================================
✓ CLI аутентифицирован
  Пользователь: user@example.com

============================================================
3. Проверка файлов конфигурации CLI
============================================================
✓ Найден: /Users/user/.claude/credentials.json
  Ключи: claudeAiOauth
  ✓ OAuth токен найден

============================================================
4. Проверка переменных окружения
============================================================
✗ ANTHROPIC_API_KEY не установлен
  SDK будет использовать CLI аутентификацию (если доступна)

============================================================
5. Тестовый вызов Claude Agent SDK
============================================================
✓ SDK найден

Выполняю тестовый запрос...
Промпт: "Ответь одним словом: ok"
  Используется: CLI аутентификация

✓ Запрос выполнен успешно!
  Время выполнения: 1234ms
  Получено сообщений: 2
  Ответ: ok
```

## Примечания

- Если `ANTHROPIC_API_KEY` установлен, SDK будет использовать его вместо CLI аутентификации
- Если CLI не аутентифицирован, SDK вызов завершится ошибкой
- Скрипт работает независимо от основного проекта Automaker
